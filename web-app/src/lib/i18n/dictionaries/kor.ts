import type { Dictionary } from "./en";

// Translations for Korean. Empty for now — every key falls back to English
// (see en.ts). Add entries as they are localised, e.g. "nav.home": "…".
export const kor: Partial<Dictionary> = {};
