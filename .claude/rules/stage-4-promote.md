# Stage 4 — Promote

1. Persist any newly confirmed rules → `docs/BUILD_RULES.md`.
2. Confirm every slice in `plan.md` is marked Done.
3. Stage for commit: `prd.md`, `plan.md`, `validation.md`, `specs/`, and the entire `prototypes/<feature>/` tree.
4. Remind user: `git push origin main` → live at the Vercel URL (~30 s).

Vercel URL pattern: see `agent.config.json → urls.vercel`.
